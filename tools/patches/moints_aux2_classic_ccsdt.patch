--- a/src/moints/moints_aux2.F
+++ b/src/moints/moints_aux2.F
@@ -1021,6 +1021,15 @@
       nvir = vhi - vlo + 1
       my_id = ga_nodeid()
       call ga_distribution(g_exch, my_id, rlo, rhi, clo, chi )
+c     Embed/classic CCSD(T) nvir=1: guard empty ownership
+      if (rhi .lt. rlo) then
+         rlo = 1
+         rhi = nbf * nvir
+      endif
+      if (chi .lt. clo) then
+         clo = 1
+         chi = 1
+      endif
       aoff = ((olo-ofroz)*(olo-ofroz-1))/2
       do a1=olo,ohi
         aa1 = a1 - ofroz
