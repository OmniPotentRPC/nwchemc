(
  basis = "sto-3g",
  theory = "scf",
  task = "energy",
  title = "encode remaining text modules as Capn Proto",
  inputStanzas = [
    ( kind = module, module = ( name = analysis ) ),
    ( kind = module, module = ( name = argos ) ),
    ( kind = module, module = ( name = argosDiana ) ),
    ( kind = module, module = ( name = argosPrep ) ),
    ( kind = module, module = ( name = argosPrepare ) ),
    ( kind = module, module = ( name = bandDplot ) ),
    ( kind = module, module = ( name = bq ) ),
    ( kind = module, module = ( name = bsemol ) ),
    ( kind = module, module = ( name = cckohn ) ),
    ( kind = module, module = ( name = cellOptimize ) ),
    ( kind = module, module = ( name = cgsd ) ),
    ( kind = module, module = ( name = cpmd ) ),
    ( kind = module, module = ( name = cpsd ) ),
    ( kind = module, module = ( name = custom ) ),
    ( kind = module, module = ( name = ddscf ) ),
    ( kind = module, module = ( name = diana ) ),
    ( kind = module, module = ( name = dimpar ) ),
    ( kind = module, module = ( name = dimqm ) ),
    ( kind = module, module = ( name = dk ) ),
    ( kind = module, module = ( name = dmd ) ),
    ( kind = module, module = ( name = dntmc ) ),
    ( kind = module, module = ( name = dplot ) ),
    ( kind = module, module = ( name = drdy ) ),
    ( kind = module, module = ( name = etrans ) ),
    ( kind = module, module = ( name = fractionalOccupations ) ),
    ( kind = module, module = ( name = freeze ) ),
    ( kind = module, module = ( name = gw ) ),
    ( kind = module, module = ( name = intgrl ) ),
    ( kind = module, module = ( name = md ) ),
    ( kind = module, module = ( name = mdXs ) ),
    ( kind = module, module = ( name = mepgs ) ),
    ( kind = module, module = ( name = metadynamics ) ),
    ( kind = module, module = ( name = mm ) ),
    ( kind = module, module = ( name = modelpotential ) ),
    ( kind = module, module = ( name = ncc ) ),
    ( kind = module, module = ( name = neb ) ),
    ( kind = module, module = ( name = occup ) ),
    ( kind = module, module = ( name = prepare ) ),
    ( kind = module, module = ( name = pspFormatter ) ),
    ( kind = module, module = ( name = pspGenerator ) ),
    ( kind = module, module = ( name = pspw ) ),
    ( kind = module, module = ( name = pspwDplot ) ),
    ( kind = module, module = ( name = pspwQmmm ) ),
    ( kind = module, module = ( name = pspwWannier ) ),
    ( kind = module, module = ( name = python ) ),
    ( kind = module, module = ( name = qmd ) ),
    ( kind = module, module = ( name = qmdNamd ) ),
    ( kind = module, module = ( name = qmmm ) ),
    ( kind = module, module = ( name = raman ) ),
    ( kind = module, module = ( name = rel ) ),
    ( kind = module, module = ( name = rism ) ),
    ( kind = module, module = ( name = rtTddft ) ),
    ( kind = module, module = ( name = smd ) ),
    ( kind = module, module = ( name = string ) ),
    ( kind = module, module = ( name = tamd ) ),
    ( kind = module, module = ( name = task ) ),
    ( kind = module, module = ( name = taskShell ) ),
    ( kind = module, module = ( name = tceMrcc ) ),
    ( kind = module, module = ( name = tddftGradient ) ),
    ( kind = module, module = ( name = tropt ) ),
    ( kind = module, module = ( name = vib ) ),
    ( kind = module, module = ( name = vibZone ) ),
    ( kind = module, module = ( name = vscf ) ),
    ( kind = module, module = ( name = waterPseudopotential ) ),
    ( kind = module, module = ( name = x2c ) ),
    ( kind = module, module = ( name = xtb ) ),
    ( kind = module, module = ( name = zora ) ),

    ( kind = task, taskStanza = ( theory = "scf", operation = "energy" ) ),
    ( kind = geometry, geometry = ( units = "angstroms", noautosym = true ) ),
    ( kind = tce, tce = ( model = "ccsd" ) ),
    ( kind = mrccData, mrccData = ( root = 1 ) ),
    ( kind = generic, generic = ( name = "prepare" ) ),
    ( kind = raw, raw = "echo" ),
    ( kind = bq, bq = ( ) ),
    ( kind = smd, smd = ( solvent = "water" ) ),
    ( kind = vib, vib = ( temperatures = [298.15] ) ),
    ( kind = dplot, dplot = ( gaussianCube = "d.cube" ) ),
    ( kind = qmd, qmd = ( nstepNucl = 10 ) ),
    ( kind = raman, raman = ( low = 100.0, high = 2000.0 ) ),
    ( kind = fon, fon = ( partial = 2 ) ),
    ( kind = neb, neb = ( nbeads = 4 ) ),
    ( kind = stringMethod, stringMethod = ( nbeads = 4 ) ),
    ( kind = gw, gw = ( ) ),
    ( kind = etrans, etrans = ( ) ),
    ( kind = rism, rism = ( ) ),
    ( kind = dimQm, dimQm = ( ) ),
    ( kind = metadynamics, metadynamics = ( ) ),
    ( kind = cellOptimize, cellOptimize = ( ) ),
    ( kind = mepgs, mepgs = ( ) ),
    ( kind = tropt, tropt = ( ) ),
    ( kind = relativistic, relativistic = ( method = zora ) ),

  ]
)
