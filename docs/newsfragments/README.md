# News Fragments

Towncrier fragments live in this directory and use the type suffixes
configured in `towncrier.toml`: `security`, `removed`, `deprecated`, `added`,
`changed`, `fixed`, `dev`, and `misc`.

Use one fragment per user-facing change.
