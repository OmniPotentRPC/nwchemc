#+TITLE: NWChem Option Mapping
#+OPTIONS: toc:nil todo:nil num:nil author:nil title:t

* Contract
~NWChemParams~ is the user-facing parameter carrier.
Every caller sends the same Cap'n Proto schema to the C ABI.
The schema provides typed fields for common runtime controls and structured
stanzas for NWChem input blocks.

* Top-Level Fields
| Field | NWChem role |
|-------+-------------|
| ~basis~ | Basis name passed to the embed bridge, for example ~sto-3g~ or ~def2-svp~. |
| ~theory~ | Main task family, for example ~scf~ or ~dft~. Short aliases such as ~blyp~ select DFT. |
| ~scfType~ | SCF type or DFT exchange-correlation keyword used by the bridge. |
| ~charge~ | Molecular charge. |
| ~multiplicity~ | Spin multiplicity, with values below one normalized to singlet for the embed call. |
| ~task~ | User task label, normally ~gradient~ for the current ABI call. |
| ~title~ | Optional NWChem title/start prefix. |
| ~memoryMb~ | Memory request in MiB; zero keeps the NWChem/runtime default. |
| ~enginePath~ | Reserved dynamic engine path; linked embed builds reject non-empty values. |
| ~nwchemRoot~ | Optional runtime root hint exported as ~NWCHEM_TOP~. |
| ~scratchDir~ | Optional scratch directory exported as ~NWCHEM_SCRATCH_DIR~. |
| ~permanentDir~ | Optional permanent directory exported as ~NWCHEM_PERMANENT_DIR~. |
| ~inputStanzas~ | Structured NWChem input blocks. |
| ~inputBlocks~ | Raw NWChem input text applied after structured stanzas. |

* Configuration Model (typed vs escape hatch)
NWChem has a very large input/RTDB surface. This project models configuration in
layers inside a single Cap'n Proto schema (~schema/Potentials.capnp~ only—no
parallel TOML/JSON/YAML config language):

1. *Top-level ~NWChemParams~ fields* — embed/ABI-critical knobs consumed by
   ~nwchemc_set_params~ / embed bridge (~basis~, ~theory~, ~charge~, dirs, ...).
2. *Structured ~NWChemInputStanza~ kinds* — high-traffic input blocks with stable
   NWChem syntax and dedicated renderers in ~src/nwchemc_params.c~.
3. *Escape hatches* — everything else remains valid NWChem configuration via:
   - ~NWChemInputStanza.raw~ / ~inputBlocks~ (literal input fragments),
   - ~NWChemSetDirective~ (~set key value~ RTDB/input directives),
   - ~NWChemModuleStanza~ with ~NWChemModuleName~ + directives, or ~customName~,
   - ~NWChemGenericStanza~ for any ~name ... end~ block not yet specialized.

The intern inventory (~schema/inventory/nwchem_features.json~) mirrors every
module enum, stanza kind, top-level field, and public ABI symbol. Regenerate
with ~pixi run inventory-gen~; validate with ~pixi run inventory-check~.

* Structured Stanzas
| Stanza | Cap'n Proto type | NWChem text emitted |
|--------+------------------+---------------------|
| Generic block | ~NWChemGenericStanza~ | ~name~, one directive per line, then ~end~. |
| Directive | ~NWChemDirective~ | ~keyword arg0 arg1 ...~ inside a block. |
| RTDB set | ~NWChemSetDirective~ | ~set key [type] value...~. |
| DFT | ~NWChemDftStanza~ | ~dft~, typed DFT controls, extra directives, then ~end~. |
| SCF | ~NWChemScfStanza~ | ~scf~, vectors/maxiter/thresh/tol2e/noprint, directives, ~end~. |
| CCSD | ~NWChemCcsdStanza~ | ~ccsd~, scalar controls and directives, then ~end~. |
| TCE | ~NWChemTceStanza~ | ~tce~, coupled-cluster/CI/MBPT/EOM controls, directives, then ~end~. |
| MRCCDATA | ~NWChemMrccDataStanza~ / ~mrccData~ | ~mrccdata~, MRCC roots, active space, references, directives, then ~end~. |
| Task line | ~NWChemTaskStanza~ | ~task [theory] [operation] [ignore]~ (explicit deck task). |
| Driver | ~NWChemDriverStanza~ | ~driver~, maxiter/tight/loose/gmax/grms/xmax/xrms/xyz, directives, ~end~. |
| Property | ~NWChemPropertyStanza~ | ~property~, dipole/mulliken/quadrupole, directives, ~end~. |
| Basis block | ~NWChemBasisStanza~ | ~basis [spherical]~, optional ~* library~, ECP line, directives, ~end~. |
| Geometry meta | ~NWChemGeometryStanza~ | ~geometry~ header (units/symmetry/noautosym); atom coords via C ABI. |
| Known module | ~NWChemModuleStanza~ | A selected NWChem module block, tokenized directives, then ~end~. |
| NWPW cutoff | ~NWChemNwpwStanza~ | ~nwpw~, cutoff controls, extra directives, then ~end~. |
| Brillouin zone | ~NWChemBrillouinZoneStanza~ / ~brillouinZone~ | ~nwpw~, ~monkhorst-pack~, optional ~brillouin_zone~, then ~end~. |
| Simulation cell | ~NWChemSimulationCellStanza~ / ~simulationCell~ | ~nwpw~, nested ~simulation_cell~, then ~end~. |
| Pseudopotential | ~NWChemPseudopotentialStanza~ | ~nwpw~, ~pseudopotentials~, library entries, extra directives, then ~end~. |
| Raw | ~NWChemInputStanza.raw~ | Literal NWChem text. |

* Direct embed apply
When ~apply_config_to_embed~ runs (embed builds only), typed
~NWChemDftStanza~, ~NWChemScfStanza~, ~NWChemCcsdStanza~,
~NWChemTceStanza~, ~NWChemDriverStanza~,
~NWChemNwpwStanza~, ~NWChemBrillouinZoneStanza~,
~NWChemSimulationCellStanza~, ~NWChemPseudopotentialStanza~, and text-valued
~NWChemSetDirective~ fields are applied outside rendered text where the bridge
has a direct RTDB/API path.

1. ~nwchemc_params_extract_direct_dft~ reads ~xc~, ~direct~, smearing.
2. ~nwchemc_params_render_embed_input_blocks~ renders stanzas with promoted DFT
   fields omitted (extra ~directives~ inside ~dft~ still render as text).
3. ~nwchemc_embed_set_dft_direct~ stores ~xc~ as the effective DFT functional
   (~cfg_scf~ / ~dft:xc~), and the legacy RTDB setup writes ~dft:direct~,
   ~int2e:filesize~, ~int2e:memsize~, ~dft:smear_sigma~, and ~dft:spinset~.

~nwchemc_params_extract_direct_set_strings~ reads text ~set key value~ stanzas.
The full deck renderer still emits ~set key value~. The embed renderer omits
those text-valued set lines and ~nwchemc_embed_set_rtdb_strings~ stores them
through ~rtdb_cput~ before method state is applied.

~NWChemSetDirective.valueType~ and ~values~ carry structured RTDB set values:
~text~ maps to NWChem's ~string~ form and ~rtdb_cput~, ~double~ maps to
~MT_DBL~, ~integer~ maps to ~MT_INT~, and ~logical~ maps to ~MT_LOG~.
~nwchemc_params_extract_direct_set_values~ and
~nwchemc_embed_set_rtdb_values~ keep those typed/list values out of
~nw_inp_from_character~ for gradient, Hessian, dipole, quadrupole, and stress
calls.
The older single ~value~ field remains valid for one text value. Literal NWChem
text remains available for parser-specific ~set~ syntax that does not fit this
typed value carrier.

~nwchemc_params_extract_direct_ccsd~ reads classic ~NWChemCcsdStanza~ scalar
controls. Full-deck rendering emits ~ccsd~ text lines; embed rendering omits
the promoted lines and writes the same values as typed RTDB entries:

| Field | NWChem text | Embed RTDB key |
|-------+-------------+----------------|
| ~maxiter~ | ~maxiter <n>~ | ~ccsd:maxiter~ |
| ~thresh~ | ~thresh <value>~ | ~ccsd:thresh~ |
| ~tol2e~ | ~tol2e <value>~ | ~ccsd:tol2e~ |
| ~iprt~ | ~iprt <n>~ | ~ccsd:iprt~ |
| ~maxDiis~ | ~diisbas <n>~ | ~ccsd:maxdiis~ |
| ~frozenCore~ | first numeric value in ~freeze~ | ~ccsd:frozen core~ |
| ~frozenVirtual~ | ~virtual <n>~ in ~freeze~ | ~ccsd:frozen virtual~ |
| ~useDisk~ | ~nodisk~ when disabled | ~ccsd:usedisk~ |
| ~sameSpinScale~ | ~fss <value>~ | ~ccsd:fss~ |
| ~oppositeSpinScale~ | ~fos <value>~ | ~ccsd:fos~ |
| ~printLevel~, ~printItems~, ~noPrintItems~ | ~print <level> <items...>~, ~noprint <items...>~ | rendered input text |
| ~doa~, ~dob~, ~dog~, ~doh~, ~dojk~, ~dos~, ~dod~ | same-named integer-list switch lines | rendered input text |
| ~useTriplesDriverNonblocking~ | ~set ccsd:use_trpdrv_nb logical <value>~ | ~ccsd:use_trpdrv_nb~ |
| ~useCcsdOpenmp~ | ~set ccsd:use_ccsd_omp logical <value>~ | ~ccsd:use_ccsd_omp~ |
| ~useTriplesDriverOpenmp~ | ~set ccsd:use_trpdrv_omp logical <value>~ | ~ccsd:use_trpdrv_omp~ |
| ~useTriplesDriverOffload~ | ~set ccsd:use_trpdrv_offload logical <value>~ | ~ccsd:use_trpdrv_offload~ |

~NWChemTceStanza~ reads the Tensor Contraction Engine coupled-cluster surface
from the same Cap'n Proto message. Full-deck rendering emits a ~tce~ block.
Embed rendering omits promoted TCE lines and writes typed ~tce:*~ RTDB entries
directly. ~method~ is the exact NWChem input token a deck should show
(examples include ~ccsd(t)~, ~cr-ccsd(t)~, ~creomsd(t)~, ~bwccsd~). For known
NWChem method tokens, the direct embed path derives the normalized RTDB fields
(~model~, ~perturbative~, ~ccsdVariant~, ~noTriplesSingles~, ~left~, and
~mrcc~ where the NWChem parser does). Explicit normalized fields override
derived method values.

| Field(s) | NWChem text | Embed RTDB key(s) |
|----------+-------------+-------------------|
| ~method~ | exact method token | derives normalized ~tce:*~ keys for known tokens |
| ~model~ | fallback method token when ~method~ is empty | ~tce:model~ |
| ~model2e~ | ~2eorb~ / ~2espin~ | ~tce:model2e~ |
| ~perturbative~ | represented by ~method~ token | ~tce:perturbative~ |
| ~ccsdVariant~, ~noTriplesSingles~ | represented by ~method~ token | ~tce:ccsdvar~, ~tce:nts~ |
| ~reference~ | ~dft~, ~hf~, or ~scf~ | ~tce:reference~ |
| ~frozenCore~, ~frozenVirtual~ | ~freeze <core> virtual <virtual>~ | ~tce:frozen core~, ~tce:frozen virtual~ |
| ~freezeMode~ | ~freeze atomic~, ~freeze core~, or ~freeze core atomic~ | rendered input text |
| ~thresh~, ~maxiter~, ~maxDiff~ | ~thresh~, ~maxiter~, ~maxdiff~ | ~tce:thresh~, ~tce:maxiter~, ~tce:maxdiff~ |
| ~levelShift~, ~leftLevelShift~ | ~lshift~, ~lshiftl~ | ~tce:zlshift~, ~tce:zlshiftl~ |
| ~levelShift2Alpha~ / ~levelShift2Beta~ | ~lshift2 <a> <b>~ | ~tce:zlshift2~ |
| ~levelShift3Alpha~ / ~levelShift3Beta~ | ~lshift3 <a> <b>~ | ~tce:zlshift3~ |
| ~ioAlgorithm~ | ~io fortran/eaf/ga/sf/replicated/dra/ga_eaf~ | ~tce:ioalg~ |
| ~diis~, ~diis2~, ~diis3~ | ~diis~, ~diis2~, ~diis3~ | ~tce:diis~, ~tce:diis2~, ~tce:diis3~ |
| ~eomSolver~, ~hbarDimension~ | ~eomsol~, ~hbard~ | ~tce:eoms~, ~tce:hbard~ |
| ~nroots~, ~target~, ~targetSymmetry~, ~symmetry~ | ~nroots~, ~target~, ~targetsym~, ~symmetry~ | ~tce:nroots~, ~tce:target~, ~tce:targetsym~, ~tce:symmetry~ |
| ~densityMatrix~, ~densityMatrixFile~, ~left~ | ~densmat [file]~ | ~tce:densmat~, ~tce:file_densmat~, ~tce:left~ |
| ~dipole~, ~quadrupole~, ~octupole~ | ~dipole~, ~quadrupole~, ~octupole~ | ~tce:left~, ~tce:multipole~ |
| ~printLevel~, ~printItems~ | ~print <level> <items...>~ | rendered input text |
| ~multipole~, ~fragment~, ~recomputeFock~ | ~multipole~, ~fragment~, ~fock~ / ~nofock~ | ~tce:multipole~, ~tce:fragment~, ~tce:recompf~ |
| ~activeOccupiedAlpha~, ~activeOccupiedBeta~ | ~active_oa~, ~active_ob~ | ~tce:active_oa~, ~tce:active_ob~ |
| ~activeVirtualAlpha~, ~activeVirtualBeta~ | ~active_va~, ~active_vb~ | ~tce:active_va~, ~tce:active_vb~ |
| ~activeOccupied~, ~activeUnoccupied~ | ~oact~, ~uact~ | ~tce:oact~, ~tce:uact~ |
| ~activeEnergyMin~, ~activeEnergyMax~ | ~emin_act~, ~emax_act~ | ~tce:eactmin~, ~tce:eactmax~ |
| ~activeExcitationLevel~ | ~t3a_lvl~ | ~tce:act_excit_lvl~ |
| ~atomicTileSize~, ~split~, ~twoElectronMethod~, ~diskBackend~ | ~attilesize~, ~split~, ~2emet~, ~idiskx~ | ~tce:maxs~, ~tce:ichopx~, ~tce:i4im~, ~tce:idiskx~ |
| ~tileSize~, ~cudaDevices~ | ~tilesize~, ~cuda~ | ~tce:tilesize~, ~tce:cuda~ |
| ~mrcc~, ~tccSpaces~, ~eaCcsd~, ~ipCcsd~ | ~tcc_spaces~, ~eaccsd~, ~ipccsd~ where accepted by NWChem input | ~tce:mrcc~, ~tce:ltcc~, ~tce:eaccsd~, ~tce:ipccsd~ |

~nwchemc_params_extract_direct_nwpw~ reads structured cutoff controls from
~NWChemNwpwStanza~. Full-deck rendering emits NWPW text lines, while embed
rendering omits the promoted cutoff lines and applies the corresponding typed
RTDB values directly:

| Field | NWChem text | Embed RTDB keys |
|-------+-------------+-----------------|
| ~energyCutoff~ | ~energy_cutoff <value>~ | ~cgsd:ecut~, ~band:ecut~, ~cpsd:ecut~, ~cpmd:ecut~ |
| ~wavefunctionCutoff~ | ~wavefunction_cutoff <value>~ | ~cgsd:wcut~, ~band:wcut~, ~cpsd:wcut~, ~cpmd:wcut~ |
| ~cutoffWavefunction~, ~cutoffEnergy~ | ~cutoff <wcut> [ecut]~; omitted ~ecut~ defaults to ~2*wcut~ | same cutoff RTDB keys |
| ~ewaldRcut~ | ~ewald_rcut <value>~ | ~cgsd:rcut~, ~band:rcut~, ~cpsd:rcut~, ~cpmd:rcut~ |
| ~ewaldNcut~ | ~ewald_ncut <value>~ | ~cgsd:ncut~, ~band:ncut~, ~cpsd:ncut~, ~cpmd:ncut~ |
| ~cellName~ | ~cell_name <name>~ | ~cgsd:cell_name~, ~band:cell_name~, ~cpsd:cell_name~ |
| ~inputWavefunctionFilename~ | ~input_wavefunction_filename <path>~ | ~cgsd:input_wavefunction_filename~, ~band:input_wavefunction_filename~, ~pspw:input vectors~, ~band:input vectors~ |
| ~outputWavefunctionFilename~ | ~output_wavefunction_filename <path>~ | ~cgsd:output_wavefunction_filename~, ~band:output_wavefunction_filename~ |
| ~fakeMass~ | ~fake_mass <value>~ | ~cgsd:fake_mass~, ~band:fake_mass~, ~cpsd:fake_mass~ |
| ~timeStep~ | ~time_step <value>~ | ~cgsd:time_step~, ~band:time_step~, ~cpsd:time_step~ |
| ~loopStart~, ~loopEnd~ | ~loop <start> <end>~ | ~cgsd:loop~, ~band:loop~, ~cpsd:loop~ |
| ~tolerancesSet~, ~toleranceEnergy~, ~toleranceDensity~, ~toleranceGradient~ | ~tolerances [e] [d] [g]~; omitted ~e~ defaults to ~1e-7~, omitted ~d~ defaults to ~e~, and omitted ~g~ defaults to ~1e-4~ | ~cgsd:tolerances~, ~band:tolerances~ |
| ~exchangeCorrelation~ | ~exchange_correlation <name>~ | ~cgsd:exchange_correlation~, ~band:exchange_correlation~, ~cpsd:exchange_correlation~, ~cpmd:exchange_correlation~ |
| ~balanceMode~ | ~balance~ or ~nobalance~ | ~nwpw:balance~ |
| ~boStepStart~, ~boStepEnd~ | ~bo_steps <start> <end>~; omitted ~end~ defaults to ~100~ | ~nwpw:bo_steps~ |
| ~mcStepStart~, ~mcStepEnd~ | ~mc_steps <start> <end>~; omitted ~end~ defaults to ~100~ | ~nwpw:bo_steps~ |
| ~boTimeStepSet~, ~boTimeStep~ | ~bo_time_step [value]~; omitted value defaults to ~15~ | ~nwpw:bo_time_step~ |
| ~boAlgorithm~ | ~bo_algorithm verlet~, ~velocity-verlet~, or ~leap-frog~ | ~nwpw:bo_algorithm~ |
| ~boFakeMassSet~, ~boFakeMass~ | ~bo_fake_mass [value]~; omitted value defaults to ~500~ | ~nwpw:bo_fake_mass~ |
| ~scalingSet~, ~scalingFirst~, ~scalingSecond~ | ~scaling [first] [second]~; omitted ~first~ defaults to ~1~ and omitted ~second~ defaults to ~first~ | ~nwpw:scaling~, ~cpmd:scaling~ |
| ~npDimensionsSet~, ~npFftProcesses~, ~npOrbitalProcesses~, ~npKspaceProcesses~ | ~np_dimensions [fft] [orbital] [k-space]~; omitted dimensions default to ~-1~ | ~nwpw:np_dimensions~ |
| ~spinOrbit~ | ~spin_orbit on~ or ~off~ | ~nwpw:spin_orbit~, ~pspw:spin_orbit~, ~band:spin_orbit~ |
| ~parallelIo~ | ~parallel_io on~ or ~off~ | ~nwpw:parallel_io~ |
| ~xyzFilename~ | ~xyz_filename <path>~ | ~cpmd:xyz_filename~, ~nwpw:xyz_filename~ |
| ~ionMotionFilename~ | ~ion_motion_filename <path>~ | ~cpmd:ion_motion_filename~, ~nwpw:ion_motion_filename~ |
| ~electronMotionFilename~ | ~emotion_filename <path>~ | ~cpmd:emotion_filename~, ~nwpw:emotion_filename~ |
| ~hamiltonianMotionFilename~ | ~hmotion_filename <path>~ | ~cpmd:hmotion_filename~, ~nwpw:hmotion_filename~ |
| ~orbitalMotionFilename~ | ~omotion_filename <path>~ | ~cpmd:omotion_filename~, ~nwpw:omotion_filename~ |
| ~eigenvalueMotionFilename~ | ~eigmotion_filename <path>~ | ~cpmd:eigmotion_filename~, ~nwpw:eigmotion_filename~ |
| ~fractionalOrbitalsStart~, ~fractionalOrbitalsEnd~ | ~fractional_orbitals <start> <end>~; omitted ~end~ defaults to ~start~ | ~nwpw:fractional_orbitals~ |
| ~smearTemperature~ | ~smear temperature <value>~; fractional orbitals append ~orbitals <start> <end>~ with defaulted ~end~ | ~nwpw:fractional_temperature~ |
| ~smearAlpha~ | ~smear alpha <value>~ | ~nwpw:fractional_alpha~ |
| ~smearType~ | ~smear fixed~, ~step~, ~fermi~, ~gaussian~, or ~marzari-vanderbilt~ | ~nwpw:fractional_smeartype~ |
| ~virtualOrbitalsStart~, ~virtualOrbitalsEnd~, ~virtualAliasSet~, ~virtualAliasStart~, ~virtualAliasEnd~ | ~virtual <start> <end>~; omitted ~end~ defaults to ~start~ | ~nwpw:excited_ne~ |
| ~lcaoMode~ | ~lcao~ or ~lcao_skip~ | ~nwpw:lcao_skip~ |
| ~ewaldGridX~, ~ewaldGridY~, ~ewaldGridZ~ | ~ewald_ngrid <x> <y> <z>~ | ~nwpw:ewald_ngrid~ |
| ~noseHoover~ | ~Nose-Hoover ...~ or ~energy~ | ~cpmd:nose~, ~nwpw:nose~ |
| ~noseRestart~ | ~restart~ or ~start~ token in ~Nose-Hoover~ | ~cpmd:nose_restart~, ~nwpw:nose_restart~ |
| ~noseElectronPeriod~ | first ~Nose-Hoover~ numeric argument | ~cpmd:Pe~, ~nwpw:Pe~ |
| ~noseElectronTemperature~ | second ~Nose-Hoover~ numeric argument | ~cpmd:Te~, ~nwpw:Te~ |
| ~noseIonPeriod~ | third ~Nose-Hoover~ numeric argument | ~cpmd:Pr~, ~nwpw:Pr~ |
| ~noseIonTemperature~ | fourth ~Nose-Hoover~ numeric argument | ~cpmd:Tr~, ~nwpw:Tr~ |
| ~noseElectronChainLength~ | sixth ~Nose-Hoover~ argument | ~cpmd:Mchain~, ~nwpw:Mchain~ |
| ~noseIonChainLength~ | seventh ~Nose-Hoover~ argument | ~cpmd:Nchain~, ~nwpw:Nchain~ |
| ~temperatureIon~, ~temperatureIonPeriod~, ~temperatureElectron~, ~temperatureElectronPeriod~, ~temperatureRestart~, ~temperatureIonChainLength~, ~temperatureElectronChainLength~ | ~temperature <Tion> <Pion> <Telc> <Pelc> <start/restart> <nchain> <mchain>~ | paired Nose-Hoover RTDB keys |
| ~atomEfield~ | ~atom_efield~ | ~nwpw:atom_efield~ |
| ~atomEfieldGradient~ | ~atom_efield_grad~ | ~nwpw:atom_efield_grad~ |
| ~mulliken~ | ~mulliken~ or ~mulliken off~ | ~cgsd:mulliken~, ~band:mulliken~, ~nwpw:mulliken~, ~cpsd:mulliken~, ~cpmd:mulliken~ |
| ~mullikenKawai~ | ~mulliken kawai~ | ~nwpw:mulliken_kawai~ |
| ~periodicDipole~ | ~periodic_dipole true~ or ~false~ | ~nwpw:periodic_dipole~ |
| ~electricField~ | ~efield true~ or ~false~ | ~nwpw:efield~ |
| ~electricFieldX~, ~electricFieldY~, ~electricFieldZ~ | ~efield true <x> <y> <z>~ | ~nwpw:efield_vector~ |
| ~electricFieldCenterSet~, ~electricFieldCenterX~, ~electricFieldCenterY~, ~electricFieldCenterZ~ | ~center <x> <y> <z>~ in ~efield~ | ~nwpw:efield_center~ |
| ~electricFieldType~ | ~periodic~, ~APC~, or ~rgrid~ in ~efield~ | ~nwpw:efield_type~ |
| ~smoothCutoff~ | ~smooth_cutoff true~ or ~false~ | ~nwpw:smooth_cutoff~ |
| ~smoothCutoffAfac~, ~smoothCutoffSigma~ | ~smooth_cutoff true <Afac> <sigma>~ | ~nwpw:smooth_cutoff~ |
| ~cutoffBootWavefunction~ | ~cutoff_boot_wavefunction true~ or ~false~ | ~nwpw:cutoff_boot_psi~ |
| ~fastErf~ | ~fast_erf true~ or ~false~ | ~nwpw:fast_erf~ |
| ~dipoleMotion~ | ~dipole_motion~ | ~nwpw:dipole_motion~ |
| ~dipoleMotionFilename~ | filename token in ~dipole_motion <filename>~ | ~nwpw:dipole_motion_filename~ |
| ~rhoUseSymmetry~ | ~symmetry true~ or ~false~ | ~nwpw:rho_use_symmetry~ |
| ~oneElectronGuessSet~, ~oneElectronGuessItIn~, ~oneElectronGuessItOut~, ~oneElectronGuessItOrtho~ | ~one_electron_guess <it_in> <it_out> <it_ortho>~; omitted counts default to ~50 1 1~ | ~nwpw:H1_it_in~, ~nwpw:H1_it_out~, ~nwpw:H1_it_ortho~ |
| ~fmm~ | ~fmm true~ or ~false~ | ~nwpw:fmm~ |
| ~fmmLmax~, ~fmmLongRange~ | ~fmm <state> <lmax> <lr>~ | ~nwpw:fmm_lmax~, ~nwpw:fmm_lr~ |
| ~born~ | ~born true~ or ~false~ | ~nwpw:born~ |
| ~bornDielectric~ | first numeric argument in ~born <dielec> ...~ | ~nwpw:born_dielec~ |
| ~bornRelax~ | ~relax true~ or ~false~ in ~born~ | ~nwpw:born_relax~ |
| ~bornVRadiiAngstrom~ | trailing input radii in ~born~ | ~nwpw:born_vradii~ in Bohr |
| ~vfieldFilenames~ | ~vfield <filename...>~ | ~nwpw:vfield_filenames~ |
| ~singlePrecisionHfx~ | ~single_precision_hfx~ | ~pspw:HFX_single_precision~ |
| ~geometryOptimize~ | ~geometry_optimize~ | ~cgsd:geometry_optimize~, ~cpsd:geometry_optimize~, ~band:geometry_optimize~ |
| ~auxiliaryPotentials~ | ~auxiliary_potentials~ | ~pspw_qmmm_auxon~ |
| ~allowTranslation~ | ~allow_translation~ | ~cgsd:allow_translation~, ~band:allow_translation~ |
| ~multiplicity~ | ~mult <value>~ | ~cgsd:ispin~, ~band:ispin~, ~cpsd:ispin~, ~cgsd:mult~, ~band:mult~, ~cpsd:mult~ |
| ~spinMode~ | ~dft~ or ~odft~ | ~cgsd:ispin~, ~band:ispin~, ~cpsd:ispin~ |
| ~dosSet~, ~dosAlphaSet~, ~dosAlpha~ | ~dos [alpha] ...~; omitted ~alpha~ defaults to ~0.05/27.2116~ | ~dos:alpha~ |
| ~dosNpoints~ | second argument in ~dos <alpha> <npoints> ...~ | ~dos:npoints~ |
| ~dosEmin~ | third argument in ~dos <alpha> <npoints> <emin> ...~ | ~dos:emin~ |
| ~dosEmax~ | fourth argument in ~dos <alpha> <npoints> <emin> <emax>~ | ~dos:emax~ |
| ~dosFilename~ | ~dos_filename <path>~ | ~nwpw:dos:filename~ |
| ~cpmdProperties~ | ~cpmd_properties true~ or ~false~ | ~nwpw:cpmd_properties~ |
| ~useGridComparison~ | ~use_grid_cmp true~ or ~false~ | ~nwpw:use_grid_cmp~ |
| ~director~ | ~director~, ~director true~, or ~director false~ | ~nwpw:use_director~ |
| ~directorFilename~ | filename token in ~director <filename>~ | ~nwpw:director_filename~ |
| ~cellExpandX~, ~cellExpandY~, ~cellExpandZ~ | ~expand_cell <x> <y> <z>~; omitted axes default to ~1~ | ~nwpw:cell_expand~ |
| ~mappingSet~, ~mapping~, ~mappingAlias~ | ~mapping [value]~, ~1d-slab~, ~2d-hilbert~, or ~2d-hcurve~; omitted ~mapping~ value defaults to ~1~ | ~nwpw:mapping~ |
| ~rotation~ | ~rotation true~ or ~false~ | ~nwpw:rotation~ |
| ~lmaxMultipole~ | ~integrate_mult_l <value>~ | ~nwpw:lmax_multipole~ |
| ~fei~ | ~Fei~ | ~cpmd:fei~, ~nwpw:fei~ |
| ~feiFilename~ | filename token in ~Fei <filename>~ | ~cpmd:fei_filename~, ~nwpw:fei_filename~ |
| ~etMovecsA~, ~etMovecsB~, ~etIonA~, ~etIonB~ | ~et <movecs-a> <movecs-b> <ion-a> <ion-b>~ | ~pspw:et:movecs_a~, ~pspw:et:movecs_b~, ~pspw:et:ion_a~, ~pspw:et:ion_b~ |
| ~initialVelocitiesTemperature~ | first argument in ~initial_velocities <temperature> <seed>~ | ~nwpw:init_velocities_temperature~, ~nwpw:init_velocities~ |
| ~initialVelocitiesSeed~ | second argument in ~initial_velocities <temperature> <seed>~ | ~nwpw:init_velocities_seed~ |
| ~makeHmass2~ | ~makehmass2 true~ or ~false~ | ~nwpw:makehmass2~ |
| ~translateVectorSet~, ~translateVectorX~, ~translateVectorY~, ~translateVectorZ~ | ~translate_vector <x> <y> <z>~; omitted ~y~ defaults to ~x~ and omitted ~z~ defaults to ~y~ | ~nwpw:translate_vector~ |
| ~translateGeometryName~ | geometry name token in ~translate_vector~ | ~nwpw:translate_geom_name~ |
| ~translateReorder~ | ~reorder~ or ~noreorder~ token in ~translate_vector~ | ~nwpw:translate_reorder~ |
| ~socketType~ | type token in ~socket <type> <ip>~ | ~nwpw:socket_type~ |
| ~socketIp~ | optional address token in ~socket <type> <ip>~ | ~nwpw:socket_ip~ |
| ~apcSet~, ~apcGc~ | ~apc <Gc> <gamma...>~ | ~nwpw_APC:Gc~, ~nwpw_APC:nga~ |
| ~apcGamma~ | trailing gamma values in ~apc~ | ~nwpw_APC:gamma~ |
| ~translation~ | ~translation true~ or ~false~ | ~cgsd:allow_translation~, ~band:allow_translation~ |
| ~minimizer~ | ~cg~, ~lmbfgs~, or ~scf~ with variant token | ~nwpw:minimizer~ |
| ~ksAlgorithm~ | ~block-cg~, ~cg~, or ~rmm-diis~ token in ~scf~ | ~nwpw:ks_algorithm~ |
| ~scfAlgorithm~ | ~simple~, ~broyden~, ~diis~, or ~anderson~ token in ~scf~ | ~nwpw:scf_algorithm~ |
| ~precondition~ | ~precondition~ or ~noprecondition~ token in ~scf~ | ~nwpw:precondition~ |
| ~kerkerG0Set~, ~kerkerG0~ | ~kerker <g0>~ token pair in ~scf~ | ~nwpw:kerker_g0~ |
| ~ksAlphaSet~, ~ksAlpha~ | ~alpha <value>~ token pair in ~scf~ | ~nwpw:ks_alpha~ |
| ~ksMaxitOrbSet~, ~ksMaxitOrb~ | ~iterations <n>~ token pair in ~scf~ | ~nwpw:ks_maxit_orb~ |
| ~ksMaxitOrbsSet~, ~ksMaxitOrbs~ | ~outer_iterations <n>~ token pair in ~scf~ | ~nwpw:ks_maxit_orbs~ |
| ~diisHistoriesSet~, ~diisHistories~ | ~diis_histories <n>~ token pair in ~scf~ | ~nwpw:diis_histories~ |

Callers still send one ~NWChemParams~ message; there is no second public config
struct.

Simple named NWPW functionals also reset the associated SIC/HFX RTDB switches
and parameters that ~nwpw_input.F~ initializes for named functionals:
~pspw:SIC_all~, ~pspw:HFX~, ~band:HFX~, ~pspw:SIC_xc_parameter~,
~pspw:SIC_h_parameter~, ~pspw:HFX_parameter~, and ~band:HFX_parameter~.
The special ~exchange_correlation new ...~ grammar remains represented with
~directives~ or raw text because it carries a nested functional definition.

* DFT Controls
~NWChemDftStanza~ provides typed fields for common DFT setup:

| Field | NWChem text |
|-------+-------------|
| ~xc~ | ~xc <value>~ |
| ~direct~ | ~direct~ |
| ~smearing.sigmaHartree~ | ~smear <sigma> fixsz~ or ~smear <sigma> nofixsz~ |
| ~directives~ | Extra tokenized DFT directives. |

Fermi smearing and related occupancy controls can also be expressed with
~NWChemSetDirective~, for example RTDB keys under ~dft:*~, or with explicit DFT
directives when the NWChem input syntax requires them.

* Module Coverage
~NWChemModuleStanza~ gives callers a typed module selector instead of a free
text block name. It covers the curated NWChem input-handler set represented in
~NWChemModuleName~, including molecular, relativistic, DFT/TDDFT, NWPW/PSPW,
optimization, property, solvation, task, utility, and XTB input handlers.

Each module stanza carries ~NWChemDirective~ entries, so a language binding can
construct NWChem input without concatenating block text. ~customName~ remains
available for NWChem modules not yet enumerated by the schema.
~tests/test_schema_module_coverage.py~ guards the enum against losing the
handler names derived from the local NWChem source tree.

* SCF Controls
~NWChemScfStanza~ covers common HF/SCF setup that complements top-level
~scfType~ (rhf/uhf/...). Typed fields:

| Field | NWChem text |
|-------+-------------|
| ~vectorsInput~ | ~vectors input <path>~ |
| ~vectorsOutput~ | ~vectors output <path>~ |
| ~maxiter~ | Full input emits ~maxiter <n>~; embed writes ~scf:maxiter~ or ~dft:iterations~ directly. |
| ~thresh~ | Full input emits ~thresh <value>~; embed writes ~scf:thresh~ directly. |
| ~tol2e~ | Full input emits ~tol2e <value>~; embed writes ~scf:tol2e~ or ~dft:tol2e~ directly. |
| ~noprint~ | ~noprint~ |
| ~directives~ | Extra SCF directives (convergence, semidirect, ...). |

* CCSD Controls
~NWChemCcsdStanza~ covers classic molecular CCSD scalar controls from
~ccsd_input.F~:

| Field | NWChem text |
|-------+-------------|
| ~maxiter~ | ~maxiter <n>~ |
| ~thresh~ | ~thresh <value>~ |
| ~tol2e~ | ~tol2e <value>~ |
| ~iprt~ | ~iprt <n>~ |
| ~maxDiis~ | ~diisbas <n>~ |
| ~frozenCore~ | first numeric value in ~freeze~ |
| ~frozenVirtual~ | ~virtual <n>~ in ~freeze~ |
| ~useDisk~ | ~nodisk~ when disabled |
| ~sameSpinScale~ | ~fss <value>~ |
| ~oppositeSpinScale~ | ~fos <value>~ |
| ~directives~ | Extra CCSD directives, including debug term switches. |

* TCE Controls
~NWChemTceStanza~ covers Tensor Contraction Engine coupled-cluster, CI, MBPT,
EOM-CC, CR-EOMCC, TCE-MRCC, perturbative triples, frozen orbitals, active
spaces, density/multipole controls, and EOM targeting through stable ~tce:*~
RTDB keys from ~tce_input.F~. Use ~directives~ for parser-only print/debug
keywords and method-specific cards that do not own a stable scalar RTDB key.
Numeric freeze counts use the direct ~tce:frozen core~ / ~tce:frozen virtual~
path. Symbolic atomic freeze modes use ~freezeMode~ and remain in rendered input
text because they do not correspond to fixed scalar counts at the C ABI boundary.
The ~dipole~, ~quadrupole~, and ~octupole~ keywords promote to
~tce:left = true~ plus the matching ~tce:multipole~ level in embed calls.

* MRCCDATA Controls
~NWChemMrccDataStanza~ covers the companion ~mrccdata~ block used by TCE MRCC
calculations through the ~mrccData~ input stanza kind. Typed fields render
~root~, ~cas~, ~nref~, reference occupation strings, ~se4t~,
~no_aposteriori~, ~subgroupsize~, ~improvetiling~, and ~usspt~. The embed path
leaves this stanza in rendered input text because the repo does not establish a
stable direct RTDB/API path for MRCC reference data. ~directives~ remain
available for extra MRCCDATA cards.

* Task / Driver / Property / Basis / Geometry Stanzas
- *Task* (~NWChemTaskStanza~): emit an explicit ~task theory operation~ line when
  building a multi-task deck. Embed callers usually rely on top-level ~theory~ +
  ~task~ only; this stanza is for full-input generation. Embed rendering omits
  task stanzas because the C ABI entry point writes ~task:theory~ and
  ~task:operation~ directly for the requested energy, gradient, force, or
  Hessian call. Dipole calls write the energy operation and read the dipole
  vector from RTDB. Quadrupole calls write the energy operation, load geometry
  and AO basis handles from RTDB, initialize the integral package for that
  basis, and call ~hnd_mtpole2~ directly for the six traceless components.
- *Driver* (~NWChemDriverStanza~): geometry optimization controls. Full input
  emits ~maxiter <n>~, ~tight~, ~loose~, and explicit ~gmax~ / ~grms~ /
  ~xmax~ / ~xrms~ values. Embed writes ~driver:nptopt~ and the
  ~driver:gmax_tol~ / ~driver:grms_tol~ / ~driver:xrms_tol~ /
  ~driver:xmax_tol~ values directly, and keeps ~xyz~ path plus extra directives
  as driver text.
- *Property* (~NWChemPropertyStanza~): common property keywords (~dipole~,
  ~mulliken~, ~quadrupole~) plus extra directives.
- *Basis* (~NWChemBasisStanza~): structured ~basis~ block when top-level ~basis~
  string alone is insufficient (spherical, segment library line, ECP token,
  per-element directives). Embed still applies top-level ~basis~ via the bridge.
- *Geometry* (~NWChemGeometryStanza~): block *metadata only* (units, symmetry,
  noautosym/noautoz/center). Nuclear coordinates always come from the C ABI
  (~positions_ang~, ~atomic_numbers~), not Cap'n Proto atom lists.

* Pseudopotentials
~NWChemNwpwStanza~ covers scalar NWPW cutoff, state, and Born-Oppenheimer
controls:

| Field | NWChem text |
|-------+-------------|
| ~energyCutoff~ | ~energy_cutoff <value>~ |
| ~wavefunctionCutoff~ | ~wavefunction_cutoff <value>~ |
| ~cutoffWavefunction~, ~cutoffEnergy~ | ~cutoff <wcut> [ecut]~ |
| ~ewaldRcut~ | ~ewald_rcut <value>~ |
| ~ewaldNcut~ | ~ewald_ncut <value>~ |
| ~cellName~ | ~cell_name <name>~ |
| ~inputWavefunctionFilename~ | ~input_wavefunction_filename <path>~ |
| ~outputWavefunctionFilename~ | ~output_wavefunction_filename <path>~ |
| ~fakeMass~ | ~fake_mass <value>~ |
| ~timeStep~ | ~time_step <value>~ |
| ~loopStart~, ~loopEnd~ | ~loop <start> <end>~ |
| ~tolerancesSet~, ~toleranceEnergy~, ~toleranceDensity~, ~toleranceGradient~ | ~tolerances [e] [d] [g]~; omitted ~e~ defaults to ~1e-7~, omitted ~d~ defaults to ~e~, and omitted ~g~ defaults to ~1e-4~ |
| ~exchangeCorrelation~ | ~exchange_correlation <name>~ |
| ~balanceMode~ | ~balance~ or ~nobalance~ |
| ~boStepStart~, ~boStepEnd~ | ~bo_steps <start> <end>~; omitted ~end~ defaults to ~100~ |
| ~mcStepStart~, ~mcStepEnd~ | ~mc_steps <start> <end>~; omitted ~end~ defaults to ~100~ |
| ~boTimeStepSet~, ~boTimeStep~ | ~bo_time_step [value]~; omitted value defaults to ~15~ |
| ~boAlgorithm~ | ~bo_algorithm verlet~, ~velocity-verlet~, or ~leap-frog~ |
| ~boFakeMassSet~, ~boFakeMass~ | ~bo_fake_mass [value]~; omitted value defaults to ~500~ |
| ~scalingSet~, ~scalingFirst~, ~scalingSecond~ | ~scaling [first] [second]~; omitted ~first~ defaults to ~1~ and omitted ~second~ defaults to ~first~ |
| ~npDimensionsSet~, ~npFftProcesses~, ~npOrbitalProcesses~, ~npKspaceProcesses~ | ~np_dimensions [fft] [orbital] [k-space]~; omitted dimensions default to ~-1~ |
| ~spinOrbit~ | ~spin_orbit on~ or ~off~ |
| ~parallelIo~ | ~parallel_io on~ or ~off~ |
| ~xyzFilename~ | ~xyz_filename <path>~ |
| ~ionMotionFilename~ | ~ion_motion_filename <path>~ |
| ~electronMotionFilename~ | ~emotion_filename <path>~ |
| ~hamiltonianMotionFilename~ | ~hmotion_filename <path>~ |
| ~orbitalMotionFilename~ | ~omotion_filename <path>~ |
| ~eigenvalueMotionFilename~ | ~eigmotion_filename <path>~ |
| ~fractionalOrbitalsStart~, ~fractionalOrbitalsEnd~ | ~fractional_orbitals <start> <end>~; omitted ~end~ defaults to ~start~ |
| ~smearTemperature~ | ~smear temperature <value>~; fractional orbitals append ~orbitals <start> <end>~ with defaulted ~end~ |
| ~smearAlpha~ | ~smear alpha <value>~ |
| ~smearType~ | ~smear fixed~, ~step~, ~fermi~, ~gaussian~, or ~marzari-vanderbilt~ |
| ~virtualOrbitalsStart~, ~virtualOrbitalsEnd~, ~virtualAliasSet~, ~virtualAliasStart~, ~virtualAliasEnd~ | ~virtual <start> <end>~; omitted ~end~ defaults to ~start~ |
| ~lcaoMode~ | ~lcao~ or ~lcao_skip~ |
| ~ewaldGridX~, ~ewaldGridY~, ~ewaldGridZ~ | ~ewald_ngrid <x> <y> <z>~ |
| ~noseHoover~ | ~Nose-Hoover ...~ or ~energy~ |
| ~noseRestart~ | ~restart~ or ~start~ token in ~Nose-Hoover~ |
| ~noseElectronPeriod~ | first ~Nose-Hoover~ numeric argument |
| ~noseElectronTemperature~ | second ~Nose-Hoover~ numeric argument |
| ~noseIonPeriod~ | third ~Nose-Hoover~ numeric argument |
| ~noseIonTemperature~ | fourth ~Nose-Hoover~ numeric argument |
| ~noseElectronChainLength~ | sixth ~Nose-Hoover~ argument |
| ~noseIonChainLength~ | seventh ~Nose-Hoover~ argument |
| ~temperatureIon~, ~temperatureIonPeriod~, ~temperatureElectron~, ~temperatureElectronPeriod~, ~temperatureRestart~, ~temperatureIonChainLength~, ~temperatureElectronChainLength~ | ~temperature <Tion> <Pion> <Telc> <Pelc> <start/restart> <nchain> <mchain>~ |
| ~directives~ | Extra tokenized NWPW directives. |

The embed path promotes these fields to the stable RTDB keys used by the NWChem
NWPW input handler and leaves only extra directives in rendered text.
Simple ~exchangeCorrelation~ names use the direct RTDB path. Use
~directives~ or raw text for ~exchange_correlation new ...~ definitions.

~NWChemBrillouinZoneStanza~ covers NWPW Brillouin-zone controls:

| Field | NWChem text | Embed RTDB key(s) |
|-------+-------------+-------------------|
| ~zoneName~ | trailing zone token in ~monkhorst-pack~ and nested ~zone_name~ | ~band:zone_name~ and key prefix ~<zone>:*~ |
| ~monkhorstPackX~, ~monkhorstPackY~, ~monkhorstPackZ~ | ~monkhorst-pack <x> <y> <z> [zone]~; omitted grid values default to ~1~ | ~<zone>:number_kvectors~, ~<zone>:kvectors~, ~<zone>:monkhorst-pack~ |
| ~dosGridX~, ~dosGridY~, ~dosGridZ~, ~dosGridZoneName~ | ~dos-grid <x> <y> <z> [zone]~; omitted grid values default to ~2~, omitted zone defaults to ~structure_default~ | ~band:dos-grid~ |
| ~tetrahedronGridX~, ~tetrahedronGridY~, ~tetrahedronGridZ~, ~tetrahedronZoneName~ | ~tetrahedron <x> <y> <z> [zone]~ | ~band:tetrahedron-grid~ |
| ~kVectors[]~ | nested ~brillouin_zone~ / ~kvector x y z weight~ entries | ~<zone>:number_kvectors~, ~<zone>:kvectors~ |
| ~maxKpointsPrint~ | nested ~max_kpoints_print <n>~ | ~<zone>:number_kvectors_print~ |
| ~zoneStructureName~ | top-level ~zone_structure_name <name>~ | ~band_structure:zone_name~ |
| ~zoneFftName~ | top-level ~zone_fft_name <name>~ | ~band_fft:zone_name~ |
| ~directives~ | Extra nested ~brillouin_zone~ directives | full-input text path |

The embed path generates Monkhorst-Pack k-vectors directly from the numeric grid
and stores the RTDB state read by NWChem's NWPW/BAND code. Explicit
~kVectors[]~ entries are also stored as numeric RTDB arrays. A zero
~weight~ maps to NWChem's omitted-weight default of ~-1.0~. Negative grid
dimensions select the NWChem no-time-reversal convention and store signed
~<zone>:monkhorst-pack~ dimensions.

~NWChemSimulationCellStanza~ covers NWPW simulation-cell controls:

| Field | NWChem text | Embed RTDB key(s) |
|-------+-------------+-------------------|
| ~cellName~ | nested ~cell_name <name>~ | key prefix ~<cell>:*~; empty uses ~cell_default:*~ |
| ~boundaryConditions~ | nested ~boundary_conditions <token>~ | ~<cell>:boundry~ |
| ~latticeVectorsBohr[9]~ | nested ~lattice_vectors~ followed by three rows | ~<cell>:unita~ |
| ~ngridX~, ~ngridY~, ~ngridZ~ | ~ngrid <x> <y> <z>~ | ~<cell>:ngrid~ |
| ~ngridSmallX~, ~ngridSmallY~, ~ngridSmallZ~ | ~ngrid_small <x> <y> <z>~ | ~<cell>:ngrid_small~ |
| ~boxDeltaBohr~ | ~box_delta <value>~ | ~<cell>:box_delta~ |
| ~boxOrient~ | ~box_orient~ | ~<cell>:box_orient~ |
| ~boxDifferentLengths~ | ~box_different_lengths~ | ~<cell>:box_type~ = 1 |
| ~latticeKind~, ~latticeLengthBohr~ | ~sc~, ~fcc~, or ~bcc~ compact lattice line | full-input text path |
| ~directives~ | Extra nested ~simulation_cell~ directives | full-input text path |

The embed path writes numeric cell state through typed RTDB values and omits the
promoted ~simulation_cell~ text block. ~latticeVectorsBohr~ is a flat 3x3 list
in Bohr and must contain either zero values or exactly nine values.

~NWChemPseudopotentialStanza~ provides a structured representation for NWPW
pseudopotential selection:

| Field | NWChem text |
|-------+-------------|
| ~entries[].element~ | Element symbol such as ~Si~ or ~O~. |
| ~entries[].libraryType~ | ~library~, ~pspw_library~, ~paw_library~, ~cpi~, or ~teter~. |
| ~entries[].libraryName~ | Library or file token used by NWChem. |
| ~entries[].allElements~ | NWChem ~*~ default entry for every element. |
| ~pspSpin~ | ~pspspin off~ for rendered text; enabled spin rules set ~nwpw:pspspin~ directly. |
| ~semicoreSmall~ | ~set nwpw:psp:semicore_small logical <value>~. |
| ~spinRules[].channel~ | ~up~ or ~down~ channel for indexed ~pspspin~ rules. |
| ~spinRules[].angularMomentum~ | ~s~, ~p~, ~d~, or ~f~ angular-momentum selector. |
| ~spinRules[].scale~ | Numeric spin-scaling factor. |
| ~spinRules[].ionIndices~ | One-based ion indices for the rule. |
| ~spinRules[].hasMagneticQuantumNumber~, ~magneticQuantumNumber~ | Optional ~pspspin not_m <m>~ prefix. |
| ~uterm~ | ~uterm off~ reset or enable direct indexed uterm promotion. |
| ~utermRules[].angularMomentum~ | ~s~, ~p~, ~d~, or ~f~ channel in indexed ~uterm~ rules. |
| ~utermRules[].uScale~, ~utermRules[].jScale~ | U/J scale values in ~uterm <l> <u> <j> ...~. |
| ~utermRules[].ionIndices~ | One-based ion indices for the uterm rule. |
| ~directives~ | Extra tokenized NWPW directives emitted after the pseudopotential block. |

The renderer emits these entries inside an ~nwpw~ block with a nested
~pseudopotentials~ section. For example, an entry with ~element = "Si"~,
~libraryType = library~, and ~libraryName = "sg15"~ renders as
~Si library sg15~.
The embed path walks the generated c-capnproto entry list through
~nwchemc_params_for_each_direct_pseudopotential~ and only packs fixed-width
records for the final C/Fortran call into NWChem. When ~allElements~ is true,
the renderer and embed direct path use NWChem's ~*~ target, which maps to
fallback RTDB keys such as ~*:pspw_library~, ~*:pspw_library_type~, and
~*:paw_library~.
~pspSpin~ is structured so callers do not need a free-form directive for the
common ~pspspin~ reset. In embed mode, ~pspSpin = disabled~ writes
~nwpw:pspspin~ = false and ~nwpw:pspspin_count~ = 0 directly, matching
~nwpw_input.F~. ~semicoreSmall~ writes the
~nwpw:psp:semicore_small~ logical key used by NWPW semicore force and stress
kernels, rendering as a top-level ~set~ directive in full decks and as a typed
RTDB value in embed mode. ~spinRules~ cover the indexed ~pspspin up/down~
spin-scaling grammar without using free-form directives. Full-deck rendering emits lines
such as ~pspspin up p 1.25 1 3~ and
~pspspin not_m 0 down d 0.75 2~. Embed rendering omits those lines and writes
~nwpw:pspspin~ = true, ~nwpw:pspspin_count~, and indexed RTDB entries with
NWChem's ~_000001~ key suffix convention:

| Rule field | Embed RTDB key pattern |
|------------+------------------------|
| channel | ~nwpw:pspspin_iamup:_000001~ |
| scale | ~nwpw:pspspin_upscale:_000001~ or ~nwpw:pspspin_downscale:_000001~ |
| angular momentum | ~nwpw:pspspin_upl:_000001~ or ~nwpw:pspspin_downl:_000001~ |
| magnetic quantum number | ~nwpw:pspspin_upm:_000001~ or ~nwpw:pspspin_downm:_000001~ |
| ion indices | ~nwpw:pspspin_upions:_000001~ or ~nwpw:pspspin_downions:_000001~ |

The direct embed path walks generated c-capnproto rule structs through
~nwchemc_params_for_each_direct_pseudopotential_spin_rule~. A stanza with
~pspSpin = disabled~ keeps reset semantics in embed mode and is not promoted as
spin-scaling rules.
~utermRules~ cover the indexed ~uterm~ potential grammar and embed as
~nwpw:uterm~ = true, ~nwpw:nuterms~, and indexed RTDB entries:

| Rule field | Embed RTDB key pattern |
|------------+------------------------|
| uScale | ~nwpw:uterm_scale:_000001~ |
| jScale | ~nwpw:jterm_scale:_000001~ |
| angular momentum | ~nwpw:uterm_l:_000001~ |
| ion indices | ~nwpw:uterm_ions:_000001~ |

* ABI Entry Points
| Symbol | Intern id | Stub | Embed |
|--------+-----------+------+-------|
| ~nwchemc_set_params~ | ~abi.nwchemc_set_params~ | fails | apply params |
| ~nwchemc_energy_gradient~ | ~abi.nwchemc_energy_gradient~ | fails | energy/gradient |
| ~nwchemc_energy~ | ~abi.nwchemc_energy~ | fails | energy-only (no grad buffer) |
| ~nwchemc_energy_forces~ | ~abi.nwchemc_energy_forces~ | fails | energy/forces (-grad) |
| ~nwchemc_hessian~ | ~abi.nwchemc_hessian~ | fails | Cartesian Hessian |
| ~nwchemc_dipole~ | ~abi.nwchemc_dipole~ | fails | total dipole |
| ~nwchemc_quadrupole~ | ~abi.nwchemc_quadrupole~ | fails | total traceless quadrupole |
| ~nwchemc_stress~ | ~abi.nwchemc_stress~ | fails | stress tensor |
| ~nwchemc_optimize~ | ~abi.nwchemc_optimize~ | fails | geometry optimization |
| ~nwchemc_frequencies~ | ~abi.nwchemc_frequencies~ | fails | harmonic frequencies |
| ~nwchemc_session_create~ | ~abi.nwchemc_session_create~ | NULL | create persistent session |
| ~nwchemc_session_set_params~ | ~abi.nwchemc_session_set_params~ | fails | replace params before topology |
| ~nwchemc_session_destroy~ | ~abi.nwchemc_session_destroy~ | no-op | release session |
| ~nwchemc_session_energy_gradient~ | ~abi.nwchemc_session_energy_gradient~ | fails | session energy/gradient |
| ~nwchemc_session_energy~ | ~abi.nwchemc_session_energy~ | fails | session energy-only |
| ~nwchemc_session_energy_forces~ | ~abi.nwchemc_session_energy_forces~ | fails | session energy/forces |
| ~nwchemc_session_dipole~ | ~abi.nwchemc_session_dipole~ | fails | session total dipole |
| ~nwchemc_session_quadrupole~ | ~abi.nwchemc_session_quadrupole~ | fails | session total traceless quadrupole |
| ~nwchemc_session_stress~ | ~abi.nwchemc_session_stress~ | fails | session stress tensor |
| ~nwchemc_session_optimize~ | ~abi.nwchemc_session_optimize~ | fails | session geometry optimization |
| ~nwchemc_session_frequencies~ | ~abi.nwchemc_session_frequencies~ | fails | session harmonic frequencies |
| ~nwchemc_session_calculate_forces~ | ~abi.nwchemc_session_calculate_forces~ | fails | ForceInput energy/forces |
| ~nwchemc_session_calculate_energy~ | ~abi.nwchemc_session_calculate_energy~ | fails | ForceInput energy-only |
| ~nwchemc_calculate_forces~ | ~abi.nwchemc_calculate_forces~ | fails | one-shot ForceInput energy/forces |
| ~nwchemc_calculate_energy~ | ~abi.nwchemc_calculate_energy~ | fails | one-shot ForceInput energy-only |
| ~nwchemc_session_calculate_result~ | ~abi.nwchemc_session_calculate_result~ | fails | ForceInput PotentialResult |
| ~nwchemc_calculate_result~ | ~abi.nwchemc_calculate_result~ | fails | one-shot ForceInput PotentialResult |
| ~nwchemc_calculate_hessian~ | ~abi.nwchemc_calculate_hessian~ | fails | one-shot ForceInput Hessian |
| ~nwchemc_hessian_result_size_for_force_input~ | ~abi.nwchemc_hessian_result_size_for_force_input~ | 0 | Hessian PotentialResult sizing |
| ~nwchemc_session_calculate_hessian_result~ | ~abi.nwchemc_session_calculate_hessian_result~ | fails | ForceInput Hessian PotentialResult |
| ~nwchemc_calculate_hessian_result~ | ~abi.nwchemc_calculate_hessian_result~ | fails | one-shot ForceInput Hessian PotentialResult |
| ~nwchemc_calculate_dipole~ | ~abi.nwchemc_calculate_dipole~ | fails | one-shot ForceInput dipole |
| ~nwchemc_dipole_result_size_for_force_input~ | ~abi.nwchemc_dipole_result_size_for_force_input~ | 0 | dipole PotentialResult sizing |
| ~nwchemc_session_calculate_dipole_result~ | ~abi.nwchemc_session_calculate_dipole_result~ | fails | ForceInput dipole PotentialResult |
| ~nwchemc_calculate_dipole_result~ | ~abi.nwchemc_calculate_dipole_result~ | fails | one-shot ForceInput dipole PotentialResult |
| ~nwchemc_calculate_quadrupole~ | ~abi.nwchemc_calculate_quadrupole~ | fails | one-shot ForceInput quadrupole |
| ~nwchemc_quadrupole_result_size_for_force_input~ | ~abi.nwchemc_quadrupole_result_size_for_force_input~ | 0 | quadrupole PotentialResult sizing |
| ~nwchemc_session_calculate_quadrupole_result~ | ~abi.nwchemc_session_calculate_quadrupole_result~ | fails | ForceInput quadrupole PotentialResult |
| ~nwchemc_calculate_quadrupole_result~ | ~abi.nwchemc_calculate_quadrupole_result~ | fails | one-shot ForceInput quadrupole PotentialResult |
| ~nwchemc_calculate_stress~ | ~abi.nwchemc_calculate_stress~ | fails | one-shot ForceInput stress |
| ~nwchemc_stress_result_size_for_force_input~ | ~abi.nwchemc_stress_result_size_for_force_input~ | 0 | stress PotentialResult sizing |
| ~nwchemc_session_calculate_stress_result~ | ~abi.nwchemc_session_calculate_stress_result~ | fails | ForceInput stress PotentialResult |
| ~nwchemc_calculate_stress_result~ | ~abi.nwchemc_calculate_stress_result~ | fails | one-shot ForceInput stress PotentialResult |
| ~nwchemc_calculate_optimize~ | ~abi.nwchemc_calculate_optimize~ | fails | one-shot ForceInput optimization |
| ~nwchemc_optimize_result_size_for_force_input~ | ~abi.nwchemc_optimize_result_size_for_force_input~ | 0 | optimization PotentialResult sizing |
| ~nwchemc_session_calculate_optimize_result~ | ~abi.nwchemc_session_calculate_optimize_result~ | fails | ForceInput optimization PotentialResult |
| ~nwchemc_calculate_optimize_result~ | ~abi.nwchemc_calculate_optimize_result~ | fails | one-shot ForceInput optimization PotentialResult |
| ~nwchemc_calculate_frequencies~ | ~abi.nwchemc_calculate_frequencies~ | fails | one-shot ForceInput frequencies |
| ~nwchemc_frequencies_result_size_for_force_input~ | ~abi.nwchemc_frequencies_result_size_for_force_input~ | 0 | frequencies PotentialResult sizing |
| ~nwchemc_session_calculate_frequencies_result~ | ~abi.nwchemc_session_calculate_frequencies_result~ | fails | ForceInput frequencies PotentialResult |
| ~nwchemc_calculate_frequencies_result~ | ~abi.nwchemc_calculate_frequencies_result~ | fails | one-shot ForceInput frequencies PotentialResult |
| ~nwchemc_session_calculate_hessian~ | ~abi.nwchemc_session_calculate_hessian~ | fails | ForceInput Hessian |
| ~nwchemc_session_calculate_dipole~ | ~abi.nwchemc_session_calculate_dipole~ | fails | ForceInput dipole |
| ~nwchemc_session_calculate_quadrupole~ | ~abi.nwchemc_session_calculate_quadrupole~ | fails | ForceInput quadrupole |
| ~nwchemc_session_calculate_stress~ | ~abi.nwchemc_session_calculate_stress~ | fails | ForceInput stress |
| ~nwchemc_session_calculate_optimize~ | ~abi.nwchemc_session_calculate_optimize~ | fails | ForceInput optimization |
| ~nwchemc_session_calculate_frequencies~ | ~abi.nwchemc_session_calculate_frequencies~ | fails | ForceInput frequencies |
| ~nwchemc_session_hessian~ | ~abi.nwchemc_session_hessian~ | fails | session Cartesian Hessian |
| ~nwchemc_available~ | ~abi.nwchemc_available~ | 0 | 1 |
| ~nwchemc_version~ | ~abi.nwchemc_version~ | stub marker | version string |
| ~nwchemc_finalize~ | ~abi.nwchemc_finalize~ | no-op | finalize runtime |

* Feature Inventory
Machine-readable intern tables:

- ~schema/inventory/nwchem_features.json~ — modules, stanzas, params fields,
  schema fields, ABI
- ~include/nwchemc_features.h~ / ~src/nwchemc_features.c~ — C intern/query API

#+begin_src bash
pixi run inventory-check
pixi run inventory-gen          # after schema edits
pixi run test-cmocka            # intern + stub suites via cmocka/pixi
meson test -C build feature-driver --print-errorlogs
#+end_src

~tools/nwchemc_feature_driver.c~ (Meson test ~feature-driver~) exercises intern
counts/lookup and stub ABI through shipped ~nwchemc_*~ symbols. Modes:
~inventory~, ~stub-abi~, ~validate~, ~all~.

Every struct field also has a ~field.<Struct>.<field>~ intern id, e.g.
~field.NWChemTceStanza.quadrupole~. The ~params.<field>~ rows remain a
top-level convenience index for ~NWChemParams~.

* Coverage Model
Typed fields cover the ABI-critical method, charge, multiplicity, directory,
DFT smearing controls, NWPW Brillouin-zone grids, and NWPW pseudopotential
library selection and semicore controls.
Known module stanzas represent the normal NWChem block vocabulary with enum
selection and tokenized directives.
The generic stanza and directive model represents arbitrary named NWChem blocks
without requiring a raw string.
The Brillouin-zone, simulation-cell, and pseudopotential stanzas cover k-point
grids (~stanza.brillouinZone~), NWPW periodic cell state
(~stanza.simulationCell~), and library/file PSP entries
(~stanza.pseudopotential~).
The raw block is kept for exact upstream syntax, generated inputs, and NWChem
keywords whose grammar is better preserved as literal input.
The intern inventory guarantees every schema enum/field/stanza/ABI symbol has a
documented intern row with stub vs embed applicability.

* Upstream / Reference Coverage Boundary
The curated ~NWChemModuleName~ enum tracks NWChem input-handler names (see
~tests/test_schema_module_coverage.py~). Method-specific option dictionaries
(every DFT grid keyword, every TCE flag, every NWPW PSPW/BAND/PAW knob) are
*not* fully typed field-by-field; callers express them with:

- ~NWChemDirective~ lists inside DFT/SCF/module/generic stanzas,
- ~NWChemSetDirective~ for RTDB-style ~set~ lines,
- ~raw~ / ~inputBlocks~ for verbatim NWChem input.

This is intentional: the Cap'n Proto contract stays logical and maintainable
while remaining complete for NWChem configuration via escape hatches.
